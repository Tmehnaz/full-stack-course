export class RecordEntry {

    // number of fields in the table
    // the data type of the fields & their access

    constructor(public id: string, public day: string, public workout_sts: string) {}   
}